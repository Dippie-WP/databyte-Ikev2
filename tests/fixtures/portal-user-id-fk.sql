-- VPN Portal — customer.user_id FK to users.id
--
-- NO-OP as of 2026-09-09 (CI fix): user_id column is now part of the
-- base customers schema (see tests/fixtures/strongswan-schema.sql /
-- tests/fixtures/quota-schema.sql), so the ALTER TABLE statement that
-- used to live here is no longer needed.
--
-- ALTER TABLE customers ADD COLUMN user_id INTEGER; was removed because
-- it caused "duplicate column name" parse errors in the DB integrity CI job.
--
-- This file is kept as a no-op so the CI workflow's sqlite3 pipe does not
-- need to be modified.
